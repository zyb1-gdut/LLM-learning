"""
ROUGE评估器实现
使用rouge库计算文本生成质量指标
包含ROUGE-1、ROUGE-2和ROUGE-L的计算

词汇覆盖评估（ROUGE-1）
短语结构评估（ROUGE-2）
句子连贯性评估（ROUGE-L）
"""

# 安装依赖: pip install rouge
from rouge import Rouge
from typing import List, Dict, Union

def calculate_rouge(
        generated_text: str,
        reference_texts: Union[str, List[str]],
        metrics: List[str] = ["rouge-1", "rouge-2", "rouge-l"]
) -> List[Dict]:
    """
    计算生成文本与参考文本之间的ROUGE分数

    参数:
        generated_text: 模型生成的文本
        reference_texts: 参考文本，可以是单个字符串或字符串列表
        metrics: 需要计算的ROUGE指标列表

    返回:
        包含各ROUGE指标分数的字典列表
    """
    # # 确保参考文本是列表格式
    # if isinstance(reference_texts, str):
    #     reference_texts = [reference_texts]

    # 初始化Rouge评估器
    rouge = Rouge()

    # 存储所有参考文本的评分结果
    all_scores = []

    # 计算每个参考文本的ROUGE分数
    for ref_text in reference_texts:
        # 计算ROUGE分数
        scores = rouge.get_scores(generated_text, ref_text)[0]
        print(f"scores：{scores}")
        # 只保留指定的指标
        # 使用字典推导式创建了一个新的字典filtered_scores。它从scores字典中筛选出键名存在于metrics列表中的项，并将这些项复制到新字典中。
        # 简单来说，就是过滤scores字典，只保留指定指标的得分。
        # 假如说scores返回值中包含了很多指标，比如rouge-1、rouge-2、rouge-l、rouge-w、rouge-s、rouge-su、rouge-n、rouge-a、rouge-b
        filtered_scores = {metric: scores[metric] for metric in metrics if metric in scores}
        print(f"filtered_scores：{filtered_scores}")
        all_scores.append(filtered_scores)

    return all_scores


def print_rouge_scores(scores_list: List[Dict]):
    """
    格式化并打印ROUGE分数

    参数:
        scores_list: ROUGE分数列表
    """
    # 打印每个参考文本的评分
    for i, scores in enumerate(scores_list, 1):
        print(f"\n===== 参考文本 {i} 的ROUGE分数 =====")
        for metric, values in scores.items():
            print(f"\n{metric.upper()} 指标:")
            print(f"  精确率 (Precision): {values['p']:.4f}")
            print(f"  召回率 (Recall):    {values['r']:.4f}")
            print(f"  F1分数:             {values['f']:.4f}")
        print("-" * 50)

if __name__ == "__main__":
    # 示例用法
    # 定义候选文本
    generated_text = "这 是 一些 生成 文本"
    # 定义参考文本
    reference_texts = [
        "这 是 一个 参考 文本", # 参考文本1
        "这 是 另外 一个 参考 文本", # 参考文本2
    ]

    print("生成文本:", generated_text)
    print("参考文本:")
    for i, ref in enumerate(reference_texts, 1):
        print(f"  参考文本 {i}: {ref}")

    # 计算ROUGE分数
    rouge_scores = calculate_rouge(
        generated_text=generated_text,
        reference_texts=reference_texts,
        metrics=["rouge-1", "rouge-2", "rouge-l"]
    )

    # 打印结果
    print("\n===== ROUGE分数详情 =====")
    print_rouge_scores(rouge_scores)

"""
ROUGE评估计算过程与结果全维度分析

 一、计算前置基础

    1. 文本分词与核心参数
    示例中所有文本已按“空格”完成分词（ROUGE计算依赖分词结果），关键信息如下：
    | 文本类型       | 分词后列表（词数）                | 核心差          |
    |---------------|--------------------------------------------------|-----------------------------|
    | 生成文本       | ["这", "是", "一些", "生成", "文本"]（5词）         | 含“一些、生成”，无“一个、参考” |
    | 参考文本1      | ["这", "是", "一个", "参考", "文本"]（5词）         | 含“一个、参考”，无“一些、生成” |
    | 参考文本2      | ["这", "是", "另外", "一个", "参考", "文本"]（6词）  | 比参考1多“另外”，比生成多“另外、一个、参考” |
    
    2. ROUGE核心计算公式
    所有指标均通过“精确率（P）、召回率（R）、F1分数”三维度评估，公式统一：
    - **精确率（P）**：匹配的词/短语数 ÷ 生成文本中词/短语总数（衡量“无冗余”）
    - **召回率（R）**：匹配的词/短语数 ÷ 参考文本中词/短语总数（衡量“无遗漏”）
    - **F1分数**：2×P×R/(P+R)（综合P和R，避免单一指标偏差）

 二、逐参考文本计算过程拆解
    代码通过循环对每个参考文本单独计算ROUGE-1、ROUGE-2、ROUGE-L，过程如下：

     （一）与参考文本1的计算过程
        1. ROUGE-1（词汇覆盖：单字词匹配）
        - **目标**：评估核心词汇是否覆盖（分层次评估第一层）
        - **计算步骤**：
          1. 找匹配单字词：生成文本与参考1的交集 → `["这", "是", "文本"]`（共3个）；
          2. 精确率（P）：3（匹配数）÷ 5（生成总词数）= 0.6；
          3. 召回率（R）：3（匹配数）÷ 5（参考1总词数）= 0.6；
          4. F1分数：2×0.6×0.6/(0.6+0.6) = 0.6。
        - **结论**：词汇覆盖良好，无明显冗余或遗漏。
        
        2. ROUGE-2（短语结构：双字词匹配）
        - **目标**：评估短语搭配是否合理（分层次评估第二层）
        - **计算步骤**：
          1. 生成文本双字词列表：`["这 是", "是 一些", "一些 生成", "生成 文本"]`（4组）；
          2. 参考1双字词列表：`["这 是", "是 一个", "一个 参考", "参考 文本"]`（4组）；
          3. 找匹配双字词：仅`["这 是"]`（1组）；
          4. 精确率（P）：1÷4 = 0.25；
          5. 召回率（R）：1÷4 = 0.25；
          6. F1分数：2×0.25×0.25/(0.25+0.25) = 0.25。
        - **结论**：短语搭配严重不合理，仅1组基础短语匹配。
        
        3. ROUGE-L（句子连贯性：最长公共子序列匹配）
        - **目标**：评估句子逻辑与结构是否连贯（分层次评估第三层）
        - **计算步骤**：
          1. 找最长公共子序列（LCS，不要求连续但保持顺序）：`["这", "是", "文本"]`（长度3）；
          2. 精确率（P）：3÷5 = 0.6；
          3. 召回率（R）：3÷5 = 0.6；
          4. F1分数：0.6。
        - **结论**：句子连贯性优秀，整体逻辑与参考1一致。

    （二）与参考文本2的计算过程
        1. ROUGE-1（词汇覆盖）
        - **计算步骤**：
          1. 匹配单字词：生成文本与参考2的交集 → `["这", "是"]`（2个）；
          2. 精确率（P）：2÷5 = 0.6（无冗余）；
          3. 召回率（R）：2÷6 ≈ 0.5（遗漏“另外、一个、参考”）；
          4. F1分数：2×0.6×0.5/(0.6+0.5) ≈ 0.5455。
        - **结论**：词汇覆盖一般，因参考2新增“另外”导致召回率下降。
        
        2. ROUGE-2（短语结构）
        - **计算步骤**：
          1. 生成文本双字词列表：同前（4组）；
          2. 参考2双字词列表：`["这 是", "是 另外", "另外 一个", "一个 参考", "参考 文本"]`（5组）；
          3. 匹配双字词：仅`["这 是"]`（1组）；
          4. 精确率（P）：1÷4 = 0.25；
          5. 召回率（R）：1÷5 = 0.2（比参考1更低，因参考2新增短语未匹配）；
          6. F1分数：2×0.25×0.2/(0.25+0.2) ≈ 0.2222。
        - **结论**：短语搭配更差，新增短语完全未覆盖。
        
        3. ROUGE-L（句子连贯性）
        - **计算步骤**：
          1. LCS：`["这", "是"]`（长度2，因“另外”等词遗漏导致变短）；
          2. 精确率（P）：2÷5 = 0.6；
          3. 召回率（R）：2÷6 ≈ 0.5；
          4. F1分数：≈0.5455。
        - **结论**：句子连贯性一般，逻辑无混乱但结构完整性下降。

 三、打印结果核心信息解读
    1. 结果结构与关键数据
    打印结果按“参考文本1→参考文本2”顺序呈现，核心数据汇总如下：
    | 参考文本 | 指标       | 精确率 | 召回率 | F1分数  |
    |----------|------------|--------|--------|---------|
    | 参考1    | ROUGE-1    | 0.6000 | 0.6000 | 0.6000  |
    | 参考1    | ROUGE-2    | 0.2500 | 0.2500 | 0.2500  |
    | 参考1    | ROUGE-L    | 0.6000 | 0.6000 | 0.6000  |
    | 参考2    | ROUGE-1    | 0.6000 | 0.5000 | 0.5455  |
    | 参考2    | ROUGE-2    | 0.2500 | 0.2000 | 0.2222  |
    | 参考2    | ROUGE-L    | 0.6000 | 0.5000 | 0.5455  |
    
    2. 跨参考文本的共性规律
    - **精确率稳定**：所有指标的精确率均为0.6（ROUGE-1/ROUGE-L）或0.25（ROUGE-2），说明生成文本“无冗余信息”，所有词/短语均围绕主题，未出现无关内容；
    - **召回率随参考文本复杂度下降**：参考2比参考1多1个核心词“另外”，导致ROUGE-1/ROUGE-L的召回率从0.6降至0.5，ROUGE-2召回率从0.25降至0.2，说明生成文本对“新增核心信息”
        的捕捉能力不足；
    - **ROUGE-2始终是短板**：两个参考文本的ROUGE-2 F1分数均<0.3，且与ROUGE-1的差距均>0.3（参考1：0.6-0.25=0.35；参考2：0.5455-0.2222≈0.32），
        完全符合代码中“短语结构不合理”的问题迹象定义。
"""